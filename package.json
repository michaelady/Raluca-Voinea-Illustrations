{
  "name": "raluca-voinea-illustration",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:docs": "vite build && node scripts/localize-pages.mjs && node scripts/sync-docs.mjs",
    "indexnow": "node scripts/notify-indexnow.mjs"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
